# Firebase Redux Login

Sample app using firebase to authenticate user.

## Setup

- Create new app in firebase console.

- Rename `*.*.sample` to `*.*`. (Remove '.sample')

- Fill blanks with your firebase app information.
